# QingYin 前端详细设计与数据映射

版本：v0.2
基准视觉：已确认的“实时会话调试”控制台。目标是高密度运营工具，而不是营销或通用数据仪表盘。

## 1. 全局框架

桌面采用固定左侧导航、顶部 Organization/Workspace/Environment 上下文选择器、主工作区和按需右侧检查器。切换 Workspace 必须刷新所有数据、清除本地缓存与未保存草稿。

| 分组 | 页面 |
| --- | --- |
| 监控中心 | 概览、实时监控、告警中心、用量统计 |
| 调试与诊断 | 实时会话调试、会话列表、事件追踪、日志检索 |
| 资源与配置 | 应用管理、路由与策略、语音资源、密钥管理、版本管理、容量管理、质量分析 |
| 系统管理 | 组织与成员、权限管理、审计日志、系统设置 |

导航按角色和 Workspace 能力显示；无权限时隐藏或给出受控空状态。

## 2. 主页面：实时会话调试

| 区块 | 用户目标 | 后端数据来源 | 权限与状态 |
| --- | --- | --- | --- |
| 顶部上下文 | 确认公司/部门/环境 | 模块 22 Workspace context | 切换后全页失效重取 |
| 会话摘要 | 识别会话、应用、状态、时长 | Session + 模块 23 Snapshot | active/completed/failed/cancelled |
| 音频轨道 | 查看实时音量包络与节奏 | 模块 23 L1/L4 telemetry | 默认低保真；无权限不显示历史波形 |
| 对话时间线 | 排查 ASR/TTS/可选 LLM 时序与错误 | 模块 12/23/24 | 内容按 L1-L5 脱敏 |
| 路由追踪 | 理解 Gateway、数据面、ASR/TTS/LLM stage 与回退 | Route decision + Provider snapshot | Provider 显示按角色脱敏 |
| 运行依据 | 查看策略版本、容量卡、Provider 快照、质量门 | 模块 10/11/21/23 | 只读版本引用，可钻取 |
| 操作区 | 取消会话、收集诊断、受控保存 | 模块 19 operation + 模块 23 | 高风险操作二次确认/审计 |

右侧检查器使用 `会话 / 运行依据 / 质量` 三标签。标签只切换检查器内容，主调试时间线保持稳定。

## 3. 关键管理页面

| 页面 | 主要内容 | 后端资源 |
| --- | --- | --- |
| 版本管理 | Policy、Provider Snapshot、Model Version 的草稿/灰度/回滚与差异 | 模块 9、11、19、21 |
| 容量管理 | Capacity Card、流量画像、safe capacity、N+1、失效原因 | 模块 7、10、19 |
| 质量分析 | 趋势、基线对比、样本量、置信度、质量门、报告状态 | 模块 21 |
| 路由策略 | 硬过滤、权重、模拟、风险、发布审批 | 模块 11、19 |
| Workspace 管理 | 成员、角色、项目、预算、地域、Provider 范围、冻结 | 模块 22、19 |

## 4. 视觉与状态规范

- 白色主工作区、深石墨导航、青绿色为正常/主操作、琥珀为降级、红色为危险；不使用渐变和装饰大图。
- 数据表、时间线和检查器以分隔线和列区分；圆角不超过 8px；禁止卡片嵌套。
- 用户文本、音频、Provider 私有名称、诊断 ID 均按数据等级显示。
- 必须覆盖 active、loading、empty、insufficient_data、degraded、failed、stale、unauthorized 与 cancelled 状态；高频事件更新不得重排关键布局。

## 5. 前端验收

所有数字、状态、按钮和链接可追溯到模块 19/21/22/23/24 的资源或 operation。Workspace 切换无跨空间残留，权限变化即时隐藏受限功能；实现阶段按已确认视觉稿逐屏比对。
