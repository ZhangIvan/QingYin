# QingYin 模块 14：生产安全与高可用恢复

版本：v0.2
目标：补齐上线前必须明确的威胁模型、信任边界、恢复目标和故障行为。以下 RTO/RPO 是初始生产设计目标，须由业务负责人确认并通过演练后才可写入 SLA。

## 1. 信任边界与资产

```text
Internet Client / SDK
  -> Edge/WAF/TLS termination
  -> QingYin public Gateway
  -> Control state / policy / secret references
  -> Provider Adapter / cloud provider / local worker
  -> metrics, logs, audit, backup systems
```

受保护资产：项目密钥、Provider 主凭证、一次性 ticket、租户策略、音频、转写文本、TTS 内容、用量/账单、审计记录、模型与镜像、容量卡和路由策略。

## 2. 威胁与强制控制

| 威胁 | 强制控制 | 验收证据 |
| --- | --- | --- |
| API Key 泄漏/越权 | 最小权限、项目/环境隔离、短生命周期 ticket、轮换与撤销、异常使用告警 | 权限矩阵、轮换和越权测试 |
| ticket 重放/劫持 | 单会话、短 TTL、nonce、来源/设备绑定（可用时）、使用后失效、传输加密 | 重放、过期、跨来源测试 |
| 音频窃听/篡改 | 公网 HTTPS/WSS，TLS 1.2 以上且优先 TLS 1.3；内部 mTLS 或工作负载身份 | TLS 扫描、证书轮换演练 |
| WebSocket 跨站滥用 | Origin allow-list、握手鉴权、消息大小/速率限制、禁止长期密钥 query 参数 | 浏览器 Origin 与 CSRF 类测试 |
| 流量耗尽/DDoS | CDN/WAF/LB 防护、连接/字节/会话多维限额、bulkhead、按租户隔离 | 洪峰和慢读压测 |
| Provider 凭证泄漏 | 专用密钥存储、引用而非明文、最小作用域、双密钥轮换、审计 | 密钥扫描、泄漏演练 |
| 数据越界/隐私违约 | `local_only` 硬过滤、数据分类、默认不留存、显式保留期与删除流程 | 路由网络审计、删除演练 |
| 日志泄露 | 结构化脱敏、禁止音频/全文/票据/密钥、受控诊断 ID、访问审计 | 日志抽样审查 |
| 供应链攻击 | 依赖锁定、SBOM、镜像签名与扫描、漏洞响应、模型哈希/许可证清单 | 构建和发布审计 |
| 管理面误操作 | RBAC、双人审批、高风险操作预览、不可变审计、策略回滚 | 权限与回滚演练 |

## 3. 密钥、数据与审计

Provider 主凭证和项目密钥的明文只允许存在于受控密钥服务的短时注入上下文。日志、指标、trace、错误、前端和 Provider 能力目录只能保存引用 ID 或脱敏摘要。

数据分级：`secret`（密钥/ticket）、`restricted`（音频/文本/账单）、`internal`（策略/容量卡）、`public`（公开 API 文档）。每级定义访问者、传输、存储、保留期、删除与导出条件。审计记录至少包含主体、动作、对象、时间、结果、trace ID、前后策略版本；审计存储需支持保留、访问控制和防篡改验证。

## 4. 高可用拓扑

- 公网 Gateway：每个生产地域至少跨两个独立故障域部署；入口负载均衡只向 ready 实例发送新会话。
- 会话：实例故障时不承诺迁移正在 Relay 的音频流。客户端收到 `session.degraded/error` 后用新 ticket 重建；Gateway 必须避免重复处理未知接收状态的音频。
- 策略、容量卡、密钥引用和租户配额：使用具备跨故障域复制与备份恢复能力的状态存储。音频流不作为控制状态保存。
- Local Worker：与 Gateway 隔离；故障 Worker 停止接新会话，按 utterance/segment 边界切云或失败。
- Provider：主备候选和独立熔断；同一外部故障域不能被错误地当作独立备份。

## 5. 初始恢复目标

| 服务范围 | 初始目标 | 恢复语义 |
| --- | --- | --- |
| 新建会话入口 | 可用性 99.95% 设计目标；单实例失效后新会话恢复不超过 60 秒 | 由多故障域 Gateway 和 LB 健康检查实现 |
| 正在进行的音频流 | 不承诺透明迁移 | 显式断开/降级，客户端按协议重连，避免重复音频 |
| 策略/容量卡/配额 | RPO 0（区域内强一致写入目标），RTO 不超过 15 分钟 | 恢复后只允许使用已验证版本 |
| 会话元数据 | RPO 不超过 60 秒，RTO 不超过 5 分钟 | 用于审计/对账，不作为音频续传依据 |
| 审计与用量 | RPO 不超过 5 分钟，RTO 不超过 24 小时 | 允许延迟汇总，不允许静默丢失 |

这些是初始设计目标，不是承诺 SLA；若业务要求更短 RTO/RPO，必须相应提高状态存储、地域拓扑、演练和成本标准。

## 6. 发布、备份与演练

发布使用不可变构件、签名版本、灰度、健康门和自动回滚。策略、模型、Adapter、容量卡和密钥引用均可独立回滚，但回滚仅影响新会话，已有 lease 需 drain。

必须定期演练：单 Gateway/Worker 故障、状态存储故障、Provider 全量不可用、错误策略发布、密钥泄漏/轮换、ticket 重放、带宽/连接耗尽、备份恢复、单地域不可用。演练要记录实际 RTO/RPO、客户可见影响、修复项与复测结果；P0 修复关闭前不可提高流量权重。

## 7. 安全与 HA 上线门

生产上线前必须通过：威胁模型评审、外部接口安全测试、密钥与日志审计、SBOM/镜像/模型完整性检查、N+1 验证、备份恢复、故障演练、RTO/RPO 实测、Provider 主备故障演练和未关闭 P0 风险清零。
