# QingYin G0 / M1-05 验收与复盘

版本：v0.1

状态：`VERIFYING`，未 `ACCEPTED`

生产判定：`NO-GO`；最高允许等级仍为 `D0 开发验证`

事实截面：2026-08-12T23:16:09+08:00

## 1. 结论

M1-05 已形成可追溯代码候选。PR #17 的已验证代码 head 为 `8fc98917cd3312660f91aa4083f62380ec73eb84`；该 SHA 上固定 Rust 1.97 的 `contract-fixtures`、`format-lint`、`unit`、`security`、`msrv` 五项 required checks 全绿，Rust 1.85 MSRV workspace check 通过。

`8fc9891` 是已取得远端证据的代码候选，不是本次 evidence-only 文档提交后的最终 PR head；新 head 必须重新通过五项 required checks。PR #17 仍为 draft、未合并且没有外部 GitHub review。三路 Codex 独立复审均为 P0=0、P1=0，但不能替代具名外部 reviewer approval。因此 G0 / M1-05 保持 `VERIFYING`，生产保持 `NO-GO`。

## 2. 范围与绑定

本轮候选范围包括：

- `qingyin-admission` 的六门准入、actor/idempotency binding、pending/reconcile、renew/release/settle/reclaim 和状态契约。
- `qingyin-testkit` 的确定性 admission store、虚拟时间、补偿、精确 TTL、generation history、租户隔离和竞态测试。
- `qingyin-security` 的 `AdmissionManage`、封闭 test fixture catalog 与非 debug profile release guard。
- 与 M1-05 同步的 Cargo lock、workspace boundary、CI/Makefile 和 delivery 状态文档。

明确不在本轮 PR 范围：

- M1-06 Provider Runtime、MockProvider profile 实现或 M1-07 Gateway 行为；这些只能做 readiness 设计。
- 用户提供的 `docs/QingYin_Codex_交接文档_2026-08-12.md`。该文件保持未修改并从 M1-05 PR 显式排除；暂存时禁止使用会扩大范围的 `git add -A`。
- 真实 PostgreSQL/Redis、真实 Provider、部署/IaC、容量、恢复、灰度或生产操作。

当前远端代码证据绑定为 PR #17 head `8fc9891`；本文件尚未提交，提交后产生的新 SHA 才是待最终核验的 PR head，不能把 `8fc9891` 误称为最终 head。

## 3. 已实现候选

- 六门顺序固定为 request rate、active sessions、gateway bytes、provider capacity、policy、budget，并覆盖每门拒绝与后续门补偿。
- admission 操作绑定完整租户、original actor、credential、principal digest、operation、reservation、request digest、session 和 exact generation。
- client cancel 仅允许 original actor；renew/release/settle/reclaim 仅允许租户匹配的 service account 与 `AdmissionManage`。
- commit/rollback 不确定结果返回 exact pending handle；compensated/expired/committed/terminal reconciliation 都携带并校验相同 identity。
- fake 覆盖精确过期、generation history/ABA、tenant-scoped budget/project capacity、renewal idempotency、终态冲突、bounded reclaim 和 release/settle race。
- security test fixtures 为封闭目录；release 及开启 debug assertions 的 release 均由非 debug Cargo profile guard 拒绝。

以上是候选实现描述，不是 GitHub 审批或生产能力证明。

## 4. 本地生成证据

执行环境：Linux server；`cargo 1.93.1`、`rustc 1.93.1`、`Python 3.14.4`。仓库固定工具链为 Rust 1.97，因此本节证据不能替代固定工具链 CI。

| evidence_id | requirement / execution | result | owner / verifier | status | binding / validity |
| --- | --- | --- | --- | --- | --- |
| `G0-RUST-FMT-001` | `cargo fmt --all --check` | `PASS` | Codex 本地执行 / 外部核验待定 | `GENERATED` | dirty worktree；Rust/Cargo/代码变化即失效 |
| `G0-RUST-CHECK-001` | `cargo check --workspace --all-targets --all-features --locked --offline` | `PASS` | Codex 本地执行 / 外部核验待定 | `GENERATED` | Rust 1.93；不能代表 pinned 1.97 或 MSRV 1.85 |
| `G0-RUST-CLIPPY-001` | workspace all-target/all-feature Clippy，`-D warnings`、locked/offline | `PASS` | Codex 本地执行 / 外部核验待定 | `GENERATED` | Rust 1.93 matching Clippy；代码/lint 变化即失效 |
| `G0-RUST-TEST-001` | `cargo test --workspace --all-targets --all-features --locked --offline` | `PASS`；78 项 unit test（admission 15、contract 4、observe 2、security 17、testkit 36、types 4；其余 0） | Codex 本地执行 / 外部核验待定 | `GENERATED` | dirty worktree；不包含 doctest |
| `G0-RUST-DOCTEST-001` | `cargo test --workspace --doc --all-features --locked --offline` | `PASS`；2 项 compile-fail doctest | Codex 本地执行 / 外部核验待定 | `GENERATED` | 证明外部不能直接 mint `Principal`/`SecurityContext` 的本地候选 |
| `G0-RUSTDOC-001` | `RUSTDOCFLAGS=-Dwarnings cargo doc --workspace --all-features --no-deps --locked --offline` | `PASS` | Codex 本地执行 / 外部核验待定 | `GENERATED` | dirty worktree；文档/API 变化即失效 |
| `G0-CUSTOM-001` | workspace boundary、contract fixture、design、Markdown link、secret regression、Cargo metadata 与 diff hygiene | `PASS` | Codex 本地执行 / 外部核验待定 | `GENERATED` | validator/fixture/docs 变化即失效并重跑 |
| `G0-SECURITY-GUARD-001` | normal release 可编译；release `test-support` 与 release+debug assertions 均以预期 guard 失败 | `PASS` | Codex 本地执行 / 外部核验待定 | `GENERATED` | release guard 或 profile 变化即失效 |
| `G0-WINDOWS-COPY-001` | CRLF/BOM、Unicode NFC、大小写碰撞、symlink、执行位、忽略垃圾和 Cargo lock 只读检查 | `PASS`；未发现需修复的平台异常 | Codex 本地执行 / 外部核验待定 | `GENERATED` | 仅绑定当前 checkout；再次复制/换机即失效 |

证据分类均为可入库的脱敏摘要；原始构建目录、终端缓存和任何凭据不得入库。

## 5. 外部与不可变证据缺口

| evidence_id | requirement | result | status | 关闭条件 |
| --- | --- | --- | --- | --- |
| `G0-COMMIT-001` | M1-05 单一、可追溯候选 binding | PR #17 已验证代码 head 为 `8fc9891`，范围不含 handoff/M1-06 行为 | `VERIFIED` | 本 evidence-only 文档提交后将产生新 head |
| `G0-PR-001` | 对应 Issue 的 M1-05 PR | PR #17 已建立、`Closes #7`，正文包含目标/非目标、rollback、验证与未关闭项 | `PENDING` | evidence-only 最终 head 五项 required checks 全绿后复核为 `VERIFIED` |
| `G0-CI-197-001` | `rust-toolchain.toml` 固定 Rust 1.97 的完整 CI | `8fc9891` 的完整 CI 全绿 | `VERIFIED` | 仅绑定 `8fc9891`；新 head 必须重跑 |
| `G0-REQUIRED-001` | branch protection required-check 配置与结果 | main 要求 `contract-fixtures`、`format-lint`、`unit`、`security`、`msrv` 五项；`8fc9891` 五项全绿 | `VERIFIED` | 最终 head 必须再次五项全绿 |
| `G0-REVIEW-001` | admission/security/state 高风险独立审阅 | 三路 Codex 复审 P0=0、P1=0；当前无外部 GitHub review | `PENDING` | agent 复核不是外部 approval；仍须具名 reviewer 审阅最终 head，并保持 discussions resolved |
| `G0-MERGE-001` | 受保护 `main` 合并与合并后回归 | PR #17 仍为 draft、未合并 | `PENDING` | 合并后记录 merge commit 与 required checks |
| `G0-MSRV-001` | `rust-version = 1.85` 的实际兼容性 | `8fc9891` 的 Rust 1.85.0 workspace all-target/all-feature locked check 通过 | `VERIFIED` | Cargo/依赖/feature/MSRV workflow 变化即重跑 |
| `G0-FINAL-HEAD-001` | evidence-only 回填后的最终 PR head | 尚未生成 | `PENDING` | 记录新 SHA，并在该 SHA 上取得五项 required checks |

`G0-PR-001`、`G0-REVIEW-001`、`G0-MERGE-001`、`G0-FINAL-HEAD-001` 未全部 `VERIFIED` 前，不允许改变 G0 状态为 `ACCEPTED`。

## 6. M1-06 / M1-07 Readiness

G0 验收期间只冻结入口，不提前实现下游行为：

1. M1-06 从已合并 M1-05 基线创建新分支；Provider registry、capability/profile snapshot 对 session 不可变，控制与执行 handle 分离，并以同一 Contract Suite 覆盖 7 个 MockProvider profile。
2. M1-07 用 authenticated tenant、canonical route、policy 和 capacity registry 在服务端构造 `AdmissionSnapshots` 与 `AdmissionDimensions`。DTO/header/metadata 不能选择或覆盖 `PolicySnapshotId`、`CapacitySnapshotId`、`GatewayPoolId`、`ProviderPoolId`、`BudgetAccountId`，必须增加注入负测。
3. M1-07 reclaimer 使用完整 tenant scope 与有界 batch。健康 store、单批积压条件下的 D0 目标为 p99 在两个调度周期内且不晚于 30 秒终态化；满批立即有界追赶，错误有界退避并暴露 oldest-expired age、reclaimed count、error/backlog。真实生产 SLO 必须在 R2/R6 重新测量与批准。

## 7. 风险与阶段决定

- 工具链声明缺口已在 `8fc9891` 上由 Rust 1.85 MSRV required lane 关闭；固定 Rust 1.97 与本地 Rust 1.93 仍是不同证据层，不能跨版本泛化结果。
- admission pool/account/snapshot 类型是公开领域类型；M1-07 若允许客户端选择这些值，会产生容量分桶、预算和策略绕过风险，必须由 trusted server resolver 封闭。
- reclaimer correctness 已有 deterministic test，但调度延迟、积压公平性和真实 backend 性能尚无环境证据。
- 本轮没有真实 Provider、真实状态 backend、部署、容量、恢复或灰度证据，生产持续 `NO-GO`。

阶段决定：G0 / M1-05 保持 `VERIFYING`。允许继续本地收口、PR/CI/审阅和 M1-06/07 readiness；不允许启动 M1-06 合并实现，不允许进入 R2，不允许任何生产操作。

## 8. 下一步

1. 提交本 evidence-only 回填并记录新的最终 PR head。
2. 在新 head 上重新取得五项 required checks。
3. 请求具名外部 reviewer 审阅最终 head，并保持 discussions resolved。
4. PR 转 ready 后合并；记录 merge commit 与合并后 required checks，再申请 G0 `ACCEPTED`。
