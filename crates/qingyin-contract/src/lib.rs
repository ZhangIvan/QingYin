//! Public control and streaming DTO validation plus fixture-facing serialization.
//!
//! Contract types depend only on canonical QingYin types, never on transport handlers or vendors.

#![forbid(unsafe_code)]
