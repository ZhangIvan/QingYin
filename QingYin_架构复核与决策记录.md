# QingYin 架构复核与决策记录

版本：v0.2
日期：2026-07-10
用途：防止早期调研结论在后续实现中被误当成已验证事实。

## 1. 已纠正的历史假设

| 编号 | 原始风险 | 当前决定 | 验证方式 |
| --- | --- | --- | --- |
| R-001 | SenseVoiceSmall 被描述为唯一“原生实时 ASR” | 仅将其列为句末增强/短分片候选；实时主链路需验证 native streaming ASR 或滑窗近实时体验 | 固定语料的 partial、final、RTF 压测 |
| R-002 | 以固定“4 核 200+ 并发”等数字估算模型容量 | 不采用任何未绑定 CPU、模型、音频与 SLA 的固定并发结论 | 目标机逐级压测后写入硬阈值 |
| R-003 | 流式需求等于模型底层必须流式 | 对外协议和体验必须流式；TTS/部分 ASR 可采用分段或滚动窗口 | 首包、gap、回改率和用户质量评测 |
| R-004 | 所有云厂商可按同一方式直连 | Direct 仅对可签安全临时凭证的厂商开放；其余走 Relay/Edge | Provider 准入探针和安全审查 |
| R-005 | 某一条固定带宽可作为系统能力上限 | Control/Relay/Worker 按各自资源独立扩展；音频大流量优先 Direct/Edge，Relay 必须按容量模型限额 | 带宽水位、容量报告和压测保护阈值 |
| R-006 | 纯 Rust 可直接解决模型并发 | Rust 优化网关、连接和尾延迟；推理容量仍由模型 RTF、CPU 与带宽决定 | Rust/Python/ONNX 同模型基准对比 |
| R-007 | 小米可直接与通用云语音厂商等价集成 | 小爱 AIVS 与 MiMo 分开；前者是设备生态通道，后者需验证通用 API 可用性 | 企业账号、协议、权限和沙箱探针 |

## 2. 架构决策记录（ADR）

### ADR-001：采用 Rust 作为生产 Gateway 基线

决定：外部连接、会话调度、限流、协议归一化和 Relay 使用 Rust；模型运行时由 Provider 选择 Rust/C API 或隔离 Worker。

原因：高连接密度和长生命周期会话更需要低内存、可控背压和明确取消语义。代价是需要维护 Rust 与模型 Worker 的契约；用 `qingyin-core` 限制这种复杂度。

### ADR-002：控制面统一，数据面可分流

决定：会话、认证、策略、计量和审计始终经过 QingYin；音频可选 Direct、Relay、Edge 或 Local。

原因：统一业务体验且不把容量不足的控制节点变成音频瓶颈。Direct 必须是 SDK 控制的受限短期会话。

### ADR-003：Provider 是唯一的扩展边界

决定：每一家云服务、每个本地模型都实现能力声明和统一 Provider trait；业务层不可调用厂商 SDK。

原因：厂商 API、认证、错误码与流式语义差异很大。该边界保障可替换性、可测性和运营一致性。

### ADR-004：实时链路快速失败而非长排队

决定：Gateway、Provider、本地模型分别设置限额；实时排队目标不超过 100 ms。

原因：语音实时性对尾延迟敏感。排长队只会把拒绝变成更晚、更差的体验。

### ADR-005：前端先设计稿后实现

决定：本阶段只冻结信息架构、状态和验收规范。高保真稿确认后，才开始前端工程。

原因：控制台是高密度运维工具，页面流程和权限状态必须在编码前确认，避免实现阶段偏离。

### ADR-006：以三份版本化协议资产冻结 v1 接口边界

决定：业务控制面、管理面和实时流分别维护 `qingyin-control-v1.yaml`、`qingyin-admin-v1.yaml` 和 `qingyin-stream-v1.yaml`。所有客户端/服务端实现必须由这些资产与模块 12 fixture 验证，不能以厂商 SDK 的对象模型作为公开契约。

原因：TTS、ASR、Realtime 的流式方向和后台资源权限不同；拆分资产能让 API 兼容治理、SDK 生成和越权测试各自保持明确。v1 仅允许兼容性增加，破坏性变更进入新主版本。

### ADR-007：空间归属链是数据、缓存和对象存储的共同安全键

决定：所有 Workspace 内实体、缓存 key、对象 key 和异步任务都以 `organization_id + workspace_id` 为强制前缀，并验证完整 `Organization -> Workspace -> Project -> Environment` 归属链。业务主体不能从请求 body 覆盖这一上下文。

原因：仅依赖前端上下文或单一资源 ID 容易造成跨部门数据、计量和诊断泄漏。统一归属键使共享基础设施仍可实现可测试的逻辑强隔离，并为专属部署保留迁移边界。

## 3. 尚未关闭的验证项

1. 每一家候选厂商的当前价格、账号并发、地域、临时凭证和商用条款。
2. 字节火山引擎与 MiMo 在目标账号下的实际流式协议及 Direct 可行性。
3. 本地 `sherpa-onnx`、SenseVoice、MeloTTS、Kokoro/Piper 在目标 CPU 上的质量与 RTF。
4. 浏览器与移动端对 Opus 容器、Direct ticket 和后台重连的兼容性。
5. 真实业务场景中“质量优先、成本优先、带宽优先”的策略权重。

这些条目未关闭前，相关能力必须保留为 `experimental` 或候选路由，不能作为默认 SLA 的依据。
